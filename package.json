{
  "name": "ltc-chamrak",
  "productName": "LTC Chamrak",
  "version": "1.0.2",
  "private": true,
  "description": "LTC Chamrak desktop app with internal Express backend and local JSON storage",
  "main": "desktop/main.js",
  "scripts": {
    "dev": "nodemon server/index.js",
    "start": "node server/index.js",
    "test": "node --test",
    "electron:dev": "electron .",
    "electron:build": "electron-builder --win"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "electron": "42.5.0",
    "electron-builder": "26.15.3",
    "nodemon": "^3.1.10"
  },
  "build": {
    "appId": "com.ltc.chamrak",
    "productName": "LTC Chamrak",
    "directories": {
      "output": "dist"
    },
    "files": [
      "desktop/**/*",
      "server/**/*",
      "web/**/*",
      "chamrak_export/**/*",
      "index.html",
      "package.json",
      "!docs/**",
      "!graphify-out/**",
      "!runtime_data/**",
      "!.git/**",
      "!.gitignore",
      "!.env",
      "!**/*.log"
    ],
    "asar": true,
    "win": {
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "artifactName": "LTC-Chamrak-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "LTC-Chamrak-Portable-${version}.${ext}"
    }
  }
}
